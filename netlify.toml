[build]
  command = "npm run build"
  publish = ".next"

[build.environment]
  NODE_VERSION = "20"

[[plugins]]
  package = "@netlify/plugin-nextjs"

# API routes (TTS, translation) run as serverless functions
[functions]
  node_bundler = "esbuild"
